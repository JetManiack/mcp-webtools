// domainPages is the extension point for product-specific pages.
// Each entry is { path: string, label: string, component: React.ComponentType }.
// An empty array means no domain pages are registered.
export const domainPages = [];
